<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ro.bluebit">

    <uses-permission android:name="android.permission.CAMERA"></uses-permission>
    <uses-permission android:name="android.permission.VIBRATE"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".ActivitateQRInformatiiTrimitere"></activity>
        <activity android:name=".InformatiiTrimitere" />
        <activity android:name=".TrimitereNouaDupaCompletareCodQR" />
        <activity android:name=".ActivitateTrimitereNoua" />
        <activity android:name=".UTILITARE.SelectieInitialaActivity" />
        <activity android:name=".Incarca_Descarca_Trimiteri_Activity" />
        <activity android:name=".Descarca_Trimiteri_Activity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data android:name="com.google.android.gms.vision.DEPENDENCIES"
            android:value="barcode"></meta-data>
    </application>

</manifest>